from seal_models import SealByteRange, DA_ALGS_T

# Generates a digest of the file (bytes)
#    digest_alg: sha256, sha512, sha1
def digest(digest_alg: DA_ALGS_T, bytes: bytes) -> bytes:
    



def SealSignLocal():
    